package StepDefinitions;

import java.util.concurrent.TimeUnit;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import io.cucumber.java.en.And;
import io.cucumber.java.en.Given;
import io.cucumber.java.en.Then;
import io.cucumber.java.en.When;

public class LoginSteps {

	/*
	 * WebDriver driver = null;
	 * 
	 * @SuppressWarnings("deprecation")
	 * 
	 * @Given("browser is open") public void browser_is_open() {
	 * 
	 * System.out.
	 * println("****************Inside Step - browser is open*******************");
	 * 
	 * String projectPath = System.getProperty("user.dir");
	 * System.out.println("Project path is : " + projectPath);
	 * 
	 * System.setProperty("webdriver.chrome.driver", projectPath +
	 * "\\src\\test\\resources\\drivers\\chromedriver.exe");
	 * 
	 * driver = new ChromeDriver(); driver.manage().window().maximize();
	 * driver.manage().timeouts().implicitlyWait(40, TimeUnit.SECONDS);
	 * driver.manage().timeouts().pageLoadTimeout(40, TimeUnit.SECONDS);
	 * 
	 * }
	 * 
	 * @And("user is on login page") public void user_is_on_login_page() {
	 * System.out.println("InsideStep -- user_is_on_login_page");
	 * 
	 * driver.navigate().to(
	 * "https://opensource-demo.orangehrmlive.com/web/index.php/auth/login"); ; }
	 * 
	 * @When("^user enters (.*) and (.*)$") public void
	 * user_enters_username_and_password(String username, String password) {
	 * System.out.println("InsideStep -- user_enters_username_and_password");
	 * 
	 * driver.findElement(By.name("username")).sendKeys(username);
	 * 
	 * driver.findElement(By.name("password")).sendKeys(password);
	 * 
	 * }
	 * 
	 * @And("clicks on login button") public void clicks_on_login_button() throws
	 * InterruptedException {
	 * System.out.println("InsideStep -- clicks_on_login_button");
	 * 
	 * driver.findElement( By.
	 * xpath("//button[@class='oxd-button oxd-button--medium oxd-button--main orangehrm-login-button']"
	 * )) .click(); Thread.sleep(4000);
	 * 
	 * }
	 * 
	 * @Then("user is navigated to the home page") public void
	 * user_is_navigated_to_the_home_page() throws InterruptedException {
	 * System.out.println("InsideStep -- user_is_navigated_to_the_home_page");
	 * 
	 * driver.getPageSource().contains("Dashboard");
	 * 
	 * Thread.sleep(6000); driver.close(); driver.quit(); }
	 */

}
